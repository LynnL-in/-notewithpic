. 38 .

### 4. 典型元件的传递函数

自动控制系统是由各种元件相互连接组成的，它们一般是机械的、电子的、液压的、光学的或其他类型的装置。为建立控制系统的数学模型，必须首先了解各种元件的数学模型及其特性。

**电位器** 电位器是一种把线位移或角位移变换为电压量的装置。在控制系统中，单个电位器用作为信号变换装置，如图 2-10(a)所示；一对电位器可组成误差检测器，如图 2-10(b)所示。

空载时，单个电位器的电刷角位移 $\theta(t)$ 与输出电压 $u(t)$ 的关系曲线如图 2-10(c)所示。图中梯形状是由绕线线径产生的误差，理论分析时可用直线近似。由图可得输出电压为
$$u(t) = K_1 \theta(t) \quad (2-43)$$
式中，$K_1 = E/\theta_{\max}$，是电刷角位移对应的输出电压，称电位器传递系数，其中 $E$ 是电位器电源电压，$\theta_{\max}$ 是电位器最大工作角。对式(2-43)求拉氏变换，并令 $U(s) = \mathscr{L}[u(t)]$，$\Theta(s) = \mathscr{L}[\theta(t)]$，可求得电位器传递函数为
$$G(s) = \frac{U(s)}{\Theta(s)} = K_1 \quad (2-44)$$
式(2-44)表明，电位器的传递函数是一个常值，它取决于电源电压 $E$ 和电位器最大工作角度 $\theta_{\max}$。电位器可用图 2-10(d)的方框图表示。

**[图 2-10 描述]**
*   **(a) 电位器：** 一个圆形电阻环，两端接电源 $E$，中间有一个可旋转的电刷，输出电压 $u(t)$ 取自电刷与电阻环的一端。
*   **(b) 桥式电位器：** 两个电位器串联，中间点作为输出，显示了 $K_1\theta_1$ 和 $K_1\theta_2$ 的关系。
*   **(c) 输出特性：** 横坐标为 $\theta(t)$，纵坐标为 $u(t)$。显示了一条通过原点的直线，但在某些区间有微小的阶梯状波动。
*   **(d) 框图表示：** 一个方框，输入为 $\Theta(s)$，输出为 $U(s)$，方框内为 $K_1$。

---

. 39 .

用一对相同的电位器组成误差检测器时，其输出电压为
$$u(t) = u_1(t) - u_2(t) = K_1[\theta_1(t) - \theta_2(t)] = K_1\Delta\theta(t)$$
式中，$K_1$ 是单个电位器的传递系数；$\Delta\theta(t) = \theta_1(t) - \theta_2(t)$ 是两个电位器电刷角位移之差，称为误差角。因此，以误差角为输入量时，误差检测器的传递函数与单个电位器传递函数相同，即为
$$G(s) = \frac{U(s)}{\Delta\Theta(s)} = K_1 \quad (2-45)$$

在使用电位器时要注意负载效应。所谓负载效应是指在电位器输出端接有负载时所产生的影响。图 2-11 表示电位器输出端接有负载电阻 $R_l$ 时的电路图，设电位器电阻是 $R_p$，可求得电位器输出电压为
$$u(t) = \frac{E}{\frac{R_p}{R_l} + \left(1 - \frac{R_p}{R_l}\right) \frac{\theta(t)}{\theta_{\max}} \left(1 - \frac{\theta(t)}{\theta_{\max}}\right)} \cdot \frac{\theta(t)}{\theta_{\max}}$$

**[图 2-11 描述]**
显示了一个电位器电路，电源 $E$ 加在电阻 $R_p$ 两端，电刷输出端连接了一个负载电阻 $R_l$。

可见，由于负载电阻 $R_l$ 的影响，输出电压 $u(t)$ 与电刷角位移 $\theta(t)$ 不再保持线性关系，因而也求不出电位器的传递函数。但是，如果负载电阻 $R_l$ 很大，如 $R_l \geqslant 10R_p$ 时，可以近似得到 $u(t) \approx E\theta(t)/\theta_{\max} = K_1\theta(t)$。因此，当电位器负载时，只是在负载阻抗足够大时，才能将电位器视为线性元件，输出电压与电刷角位移之间才有线性关系。

**测速发电机** 测速发电机是用于测量角速度并将它转换成电压量的装置。在控制系统中常用的有直流和交流测速发电机，如图 2-12 所示。图 2-12(a)是永磁式直流测速发电机的原理线路图。测速发电机的转子与待测量的轴相连接，在电枢两端输出与转子角速度成正比的直流电压，即
$$u(t) = K_t\omega(t) = K_t\frac{d\theta(t)}{dt} \quad (2-46)$$
式中，$\theta(t)$ 是转子角位移；$\omega(t) = d\theta(t)/dt$ 是转子角速度；$K_t$ 是测速发电机输出斜率，表示单位角速度的输出电压。在零初始条件下，对式(2-46)求拉氏变换可得直流测速发电机的传递函数为
$$G(s) = \frac{U(s)}{\Omega(s)} = K_t \quad (2-47)$$
或
$$G(s) = \frac{U(s)}{\Theta(s)} = K_ts \quad (2-48)$$
式中，$U(s) = \mathscr{L}[u(t)]$；$\Omega(s) = \mathscr{L}[\omega(t)]$；$\Theta(s) = \mathscr{L}[\theta(t)]$。式(2-47)和式(2-48)可分别用图 2-13 中的两个方框图表示。

---

. 40 .

**[图 2-13 描述]**
(a) 框图：输入 $\Omega(s)$，经过 $K_t$ 方框，输出 $U(s)$。
(b) 框图：输入 $\Theta(s)$，经过 $sK_t$ 方框，输出 $U(s)$。

图 2-12(b)是交流测速发电机的示意图。在结构上有两个互相垂直放置的线圈，其中一个是激磁绕组，接入一定频率的正弦额定电压，另一个是输出绕组。当转子旋转时，输出绕组产生与转子角速度成比例的交流电压 $u(t)$，其频率与激磁电压频率相同，其包络线也可以用式(2-46)表示，因此其传递函数及方框图亦同直流测速发电机。

**电枢控制直流伺服电动机** 电枢控制的直流伺服电动机在控制系统中广用作执行机构，用来对被控对象的机械运动实现快速控制。根据式(2-39)和式(2-40)可用图 2-14 的方框图表示三种情况下的电枢控制直流伺服电动机。

**[图 2-14 描述]**
(a) 框图：输入 $U_a(s)$，经过 $\frac{K_m}{T_ms+1}$，输出 $\Omega(s)$。
(b) 框图：输入 $U_a(s)$，经过 $\frac{K_m}{s(T_ms+1)}$，输出 $\Theta(s)$。
(c) 框图：输入 $M_e(s)$，经过 $\frac{-K_c}{T_ms+1}$，输出 $\Omega(s)$。

**两相伺服电动机** 两相伺服电动机具有重量轻、惯性小、加速特性好的优点，是控制系统中广泛应用的一种小功率交流执行机构。
两相伺服电动机由互相垂直配置的两相定子线圈和一个高电阻值的转子组成。定子线圈的一相是激磁绕组，另一相是控制绕组，通常在功率放大器的输出端，提供数值和极性可变的交流控制电压。
两相伺服电动机的转矩-速度特性曲线有斜率，且呈非线性。图 2-15(b)是在不同控制电压 $u_c$ 时，实验取的一组机械特性曲线。考虑到在控制系统中，伺服电动机一般工作在零转速附近，作为线性化的一种方法，通常把低速部分的线性段延伸到高速范围，用低速直线近似代替非线性特性，如图 2-15(b)中虚线所示。此外，也可用小偏差...

**[图 2-15 描述]**
(a) 两相伺服电动机：显示电机符号 SM。
(b) 电动机的转矩-速度特性：横坐标为速度 $\times 10^3$ (r/min)，纵坐标为转矩 $M_m \times 10^{-2}$ (N·m)。图中画出了 $u_c=25V, 50V, 75V, 100V$ 的四条曲线，显示了转矩随速度增加而减小的非线性关系，并有对应的线性化虚线。