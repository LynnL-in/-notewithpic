支路增益是 1，如图 2-34(b)所示。

[图 2-34 比较点与节点对应关系]
(a) 比较点前无引出点时的节点设置：左侧为一个输入节点，经过 $G_1$ 模块后分为 $e_1$ 和 $e_3$ 两条支路，分别连接到 $G_3$ 和 $G_4$ 模块。右侧为对应的信号流图，节点 $G_1$ 指向 $e_1$ 和 $e_3$。
(b) 比较点前有引出点时的节点设置：左侧为一个输入节点，经过 $G_1$ 模块后，引出点 $e_1$ 分出支路 $e_3$ 连接到 $G_3$ 和 $G_2$ 模块，同时 $G_1$ 输出连接到 $G_4$。右侧为对应的信号流图，显示了 $G_1$ 到 $e_1$ 的支路，以及 $e_1$ 到 $G_4$、 $e_1$ 到 $e_3$ 的支路，其中 $e_3$ 处有反馈回路 $-G_2$。

例 2-13 试绘制图 2-35 所示系统结构图对应的信号流图。

[图 2-35 例 2-13 系统的结构图]
该图显示了一个闭环控制系统：输入 $R(s)$ 经过比较点，与反馈信号 $H(s)$ 相减，经过 $G_1(s)$，再经过一个比较点（与 $G_2(s)$ 的反馈相加），最后经过 $G_3(s)$ 输出 $C(s)$。其中 $G_4(s)$ 构成另一个反馈回路。

解 首先，在系统结构图的信号线上，用小圆圈标注各变量对应的节点，如图 2-36(a)所示。其次，将各节点按原顺序自左向右排列，连接各节点的支路与结构图中的方框相对，即将结构图中的方框用具有相应增益的支路代替，并连接有关的节点，便得到系统的信号流图，如图 2-36(b)所示。

[图 2-36 例 2-13 系统的结构与信号流图]
(a) 结构图：展示了 $R(s)$ 到 $C(s)$ 的完整结构，包含 $G_1(s), G_2(s), G_3(s), G_4(s)$ 和 $H(s)$。
(b) 信号流图：将结构图转化为节点和支路，支路增益分别为 $1, G_1(s), 1, G_3(s), -H(s), G_2(s), G_4(s)$。

---

### 5. 梅森增益公式

从一个复杂的系统信号流图上，经过简化可以求出系统的传递函数，而且，结构图的等效变换规则亦适用于信号流图的简化，但这个过程毕竟是很麻烦的。控制工程中常应用梅森(Mason)增益公式直接求从源节点到阱节点的传递函数，而不需简化信号流图，这就为信号流图的广泛应用提供了方便。当然，由于系统结构图与信号流图之间有对应关系，梅森增益公式也可直接用于系统结构图。

梅森增益公式的来源是按克莱姆(Cramer)法则求解线性联立方程式组时，将解的分子多项式及分母多项式与信号流图(即拓扑图)巧妙联系的结果。

在图 2-37 的典型信号流图中，变量 $U_i$ 和 $U_o$ 分别用源节点 $U_i$ 和阱节点 $U_o$ 表示，由图可得相应的一组代数方程为
$$X_1 = aU_i + fX_2$$
$$X_2 = bX_1 + gX_3$$
$$X_3 = cX_2 + hX_4$$
$$X_4 = dX_3 + eU_i$$
$$U_o = X_4$$

[图 2-37 典型信号流图]
该图包含节点 $U_i, X_1, X_2, X_3, X_4, U_o$。支路增益分别为：$U_i \to X_1$ 为 $a$，$X_1 \to X_2$ 为 $b$，$X_2 \to X_3$ 为 $c$，$X_3 \to X_4$ 为 $d$，$X_4 \to U_o$ 为 $1$。反馈支路：$X_2 \to X_1$ 为 $f$，$X_3 \to X_2$ 为 $g$，$X_4 \to X_3$ 为 $h$，$X_4 \to X_1$ 为 $e$。

整理后得
$$X_1 - fX_2 = aU_i$$
$$bX_1 - X_2 + gX_3 = 0$$
$$cX_2 - X_3 + hX_4 = 0$$
$$-dX_3 + X_4 = eX_i$$

现在用克莱姆法则求解上述方程组的解 $X_4$(即变量 $U_o$)，并进而求出系统的传递函数 $U_o/U_i$。由克莱姆法则，方程组的系数行列式为
$$\Delta = \begin{vmatrix} 1 & -f & 0 & 0 \\ b & -1 & g & 0 \\ 0 & c & -1 & h \\ 0 & 0 & -d & 1 \end{vmatrix} = 1 - dh - gc - fb + fbdh \quad (2-75)$$
$$\Delta_4 = \begin{vmatrix} 1 & -f & 0 & aU_i \\ b & -1 & g & 0 \\ 0 & c & -1 & 0 \\ 0 & 0 & -d & eU_i \end{vmatrix} = abcdU_i + eU_i(1 - gc - bf) \quad (2-76)$$

因此，$X_4 = U_o = \Delta_4/\Delta$，即有
$$\frac{U_o}{U_i} = \frac{X_4}{U_i} = \frac{abcd + e(1 - gc - bf)}{1 - dh - gc - fb + fbdh} \quad (2-77)$$

对上述传递函数的分母多项式及分子多项式进行分析后，可以得到它们与系数行列式 $\Delta, \Delta_4$ 及信号流图之间的巧妙联系。首先可以发现，传递函数的分母多项式即是系数行列式 $\Delta$，而且其中包含有信号流图中的三个单独回路增益之和，即 $-(fb + gc + dh)$，以及两个不接触的回路增益之乘积，即 $fbdh$。这个特点可以用信号流图的名词术语写成如下形式：
$$\Delta = 1 - \sum L_a + \sum L_b L_c \quad (2-78)$$
式中，$\sum L_a$ 表示信号流图中所有单独回路的回路增益之和，即 $\sum L_a = fb + gc + dh$；$\sum L_b L_c$ 表示信号流图中每两个互不接触的回路增益之乘积的项，即 $\sum L_b L_c = fbdh$。

其次可以看到，传递函数的分子多项式与系数行列式 $\Delta_4$ 相对应，而且其中包含有两条前向通路总增益之和，即 $abcd + e$，以及与前向通路 $e$ 不接触的两个单独回路的回路增益与该前向通路总增益之乘积的项，即 $-(gce + bfe)$。这个特点也可以用信号流图的名词术语写成如下形式：
$$\frac{\Delta_4}{U_i} = \sum_{k=1}^2 p_k - \sum_{i=2} p_i L_i \quad (2-79)$$

式中，$p_k$ 是第 $k$ 条前向通路总增益，本例中共有两条前向通路，故 $\sum p_k = p_1 + p_2 = abcd + e$；$L_i$ 为与第 $i$ 条前向通路不接触回路的回路增益，本例中有两个回路与第二条前向通路不接触，故 $\sum p_2 L_2 = gce + bfe$。进一步分析还可以发现 $L_i$ 与系数行列式 $\Delta$ 之间有着微妙的联系，即 $L_i$ 是系数行列式 $\Delta$ 中与第 $i$ 条前向通路不接触的所有回路的回路增益项。例如，第二条前向通路 $e$ 与回路增益为 $gc$ 和 $bf$ 的两个回路均不接触，它正好是系数行列式 $\Delta$ 中的两项 $-(gc + fb)$。若前向通路与所有回路都接触，则 $L_i = 0$。现令 $\Delta_i = 1 - L_i$，则传递函数分子多项式还可进一步简记为
$$\frac{\Delta_4}{U_i} = \sum_{k=1}^2 p_k \Delta_k \quad (2-80)$$

式中，$\Delta_k$ 是与第 $k$ 条前向通路对应的余因子，它等于系数行列式 $\Delta$ 中，去掉与第 $k$ 条前向通路接触的所有回路的回路增益项后的余项。本例中，$k=1$ 时，$p_1 = abcd, \Delta_1 = 1$；$k=2$ 时，$p_2 = e, \Delta_2 = 1 - gc - bf$。于是，使用信号流图的名词术语后，式(2-77)系统传递函数可写为
$$\frac{U_o}{U_i} = \frac{p_1 \Delta_1 + p_2 \Delta_2}{\Delta} = \frac{1}{\Delta} \sum_{k=1}^2 p_k \Delta_k \quad (2-81)$$

该表达式建立了信号流图的某些特征(如前向通路总增益、回路增益等)与系统传递函数(或输出量)之间的直观联系，这就是梅森增益公式。根据这个公式，可以从信号流图直接写出从源节点到阱节点的传递函数表达式。

推而广之，具有任意条前向通路及任意个单独回路和不接触回路的复杂信号流图，求从任意节点到任意阱节点之间传递函数的梅森增益公式记为
$$P = \frac{1}{\Delta} \sum_{k=1}^n p_k \Delta_k \quad (2-82)$$

式中，$P$ 为从源节点到阱节点的传递函数(或总增益)；$n$ 为从源节点到阱节点的前向通路总数；$p_k$ 为从源节点到阱节点的第 $k$ 条前向通路总增益；$\Delta = 1 - \sum L_a + \sum L_b L_c - \sum L_d L_e L_f + \cdots$ 称为流图特征式，其中 $\sum L_a$ 为所有单独回路增益之和；$\sum L_b L_c$ 为所有互不接触的单独回路中，每次取其中两个回路的回路增益的乘积之和；$\sum L_d L_e L_f$ 为所有互不接触的单独回路中，每次取其中三个回路的回路增益的乘积之和；$\Delta_k$ 为流图余因子，它等...