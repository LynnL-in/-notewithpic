校正网络的 $L_c(\omega)$ 和已校正系统的 $L''(\omega)$ 已绘于图 6-22 之中。

[图 6-22 描述：该图展示了对数幅频特性曲线。横坐标为频率 $\omega$，范围从 0.01 到 100。纵坐标为 $L(\omega)$，范围从 -80 到 80。图中包含三条曲线：$L(\omega)$（未校正系统）、$L_c(\omega)$（校正网络）和 $L''(\omega)$（已校正系统）。图中标记了关键点：在 $\omega=1.0$ 处，$1/bT=0.27$，$1/T=0.024$，$\omega_c''=2.7$。在 $\omega_c''$ 处，相位角 $\gamma(\omega_c'')=46.5^\circ$。图中还显示了斜率变化，包括 -20dB/dec 和 -40dB/dec 的段落。]

图 6-22 例 6-4 系统开环对数幅频特性

最后校验相角裕度和幅值裕度。由式(6-27)及 $b=0.09$ 算得 $\varphi_c(\omega_c'')=-5.2^\circ$，于是求出 $\gamma''=41.3^\circ$，满足指标要求。然后用试算法可得已校正系统对数相频特性为 $-180^\circ$ 时的频率为 $6.8\text{rad/s}$，求出已校正系统的幅值裕度为 $10.5\text{dB}$，完全符合要求。

采用串联滞后校正，既能提高系统稳态精度，又基本不改变系统动态性能的原因是明显的。以图 6-22 为例，如果将已校正系统对数幅频特性向上平移 $21\text{dB}$，则校正前后相角裕度和截止频率基本相同，但开环增益却增大 11 倍。

串联滞后校正与串联超前校正两种方法，在完成系统校正任务方面是相同的，但有以下不同之处：

1) 超前校正是利用超前网络的相角超前特性，而滞后校正是利用滞后网络的高频幅值衰减特性。

2) 为了满足严格的稳态性能要求，当采用无源校正网络时，超前校正要求一定的附加增益，而滞后校正一般不需要附加增益。

3) 对于同一系统，采用超前校正的系统带宽大于采用滞后校正的系统带宽。从提高系统响应速度的观点来看，希望系统带宽越大越好；与此同时，带宽越大系统越易受噪声干扰的影响，因此如果系统输入端噪声电平较高，一般不宜选用超前校正。

最后指出，在有些应用方面，采用滞后校正可能会出时间常数大到不能实现的结果。这种不良后果的出现，是由于需要在足够小的频率值上安置滞后网络第一个交接频率 $1/T$，以保证在需要的频率范围内产生有效的高频幅值衰减特性所致。在这种情况下，最好采用串联滞后-超前校正。

### 4. 串联滞后-超前校正

这种校正方法兼有滞后校正和超前校正的优点，即已校正系统响应速度较快，超调量较小，抑制高频噪声的性能也较好。当待校正系统不稳定，且要求校正后系统的响应速度、相角裕度和稳态精度较高时，以采用串联滞后-超前校正为宜。其基本原理是利用滞后-超前网络的前超前部分增大系统的相角，同时利用滞后部分来改善系统的稳态性能。串联滞后-超前校正的设计步骤如下：

1) 根据稳态性能要求确定开环增益 $K$。

2) 绘制待校正系统的开环对数幅频特性，求出待校正系统的截止频率 $\omega_c'$ 相角裕度 $\gamma$ 和幅值裕度 $h(\text{dB})$。

3) 在待校正系统开环对数幅频特性上，选择斜率从 $-20\text{dB/dec}$ 变为 $-40\text{dB/dec}$ 的交接频率作为校正网络前部分的交接频率 $\omega_b$。

$\omega_b$ 的这种选法，可以降低已校正系统的阶次，且可保证中频斜率为期望的 $-20\text{dB/dec}$，并占据较宽的频带。

4) 根据响应速度要求，选择系统的截止频率 $\omega_c''$ 和校正网络衰减因子 $1/\alpha$。要保证已校正系统的截止频率为所选的 $\omega_c''$，下列等式应成立：
$$-20\lg\alpha + L'(\omega_c'') + 20\lg T_b\omega_c'' = 0 \quad (6-43)$$
式中，$T_b=1/\omega_b$; $L'(\omega_c'') + 20\lg T_b\omega_c''$ 可由待校正系统开环对数幅频特性的 $-20\text{dB/dec}$ 延长线在 $\omega_c''$ 处的数值确定。因此，由式(6-43)可以求出 $\alpha$ 值。

5) 根据相角裕度要求，估算校正网络滞后部分的交接频率 $\omega_a$。

6) 校验已校正系统的各项性能指标。

**例 6-5** 设待校正系统开环传递函数为
$$G_0(s) = \frac{K_v}{s\left(\frac{1}{6}s+1\right)\left(\frac{1}{2}s+1\right)}$$
要求设计校正装置，使系统满足下列性能指标：
1) 在最大指令速度为 $180^\circ/\text{s}$ 时，位置滞后误差不超过 $1^\circ$。
2) 相角裕度为 $45^\circ \pm 3^\circ$。
3) 幅值裕度不低于 $10\text{dB}$。
4) 动态过程调节时间不超过 $3\text{s}$。

**解** 首先确定开环增益。由题意，取
$$K = K_v = 180\text{s}^{-1}$$
作待校正系统对数幅频特性 $L'(\omega)$，如图 6-23 所示。图中，最低频段为 $-20\text{dB/dec}$ 斜率直线，其延长线交 $\omega$ 轴于 $180\text{rad/s}$，该值即 $K_v$ 的数值。由图得待校正系统截止频率 $\omega_c'=12.6\text{rad/s}$，算出待校正系统的相角裕度 $\gamma=-55.5^\circ$，幅值裕度 $h=-30\text{dB}$，表明待校正系统不稳定。

由于待校正系统在截止频率处的相角远小于 $-180^\circ$，且响应速度有一定要求，故应优先考虑采用串联滞后-超前校正。论证如下：

首先，考虑采用串联超前校正。要把待校正系统的相角裕度从 $-55.5^\circ$ 提高到 $45^\circ$，至少选用两级串联超前网络。显然，校正后系统的截止频率将过大，可能超过 $25\text{rad/s}$。从理论上说，截止频率越大，则系统的响应速度越快。譬如说，在 $\omega_c''=25\text{rad/s}$ 时，系统动态过程的调节时间近似为 $0.34\text{s}$，这将比性能指标要求提高 10 倍，然而进一步分析发现：①伺服电机将出现速度饱和，这是因为超前校正系统使伺服机构输出的变化速率超过了伺服电机的最大输出转速之故，于是，$0.34\text{s}$ 的调节时间将变得毫无意义；②系统带宽过大，造成输出噪声电平过高；③需要附加前置放大器，使系统结构复杂化。

[图 6-23 描述：该图展示了例 6-5 的系统开环对数幅频特性。横坐标为频率 $\omega$，范围从 0.01 到 100。纵坐标为 $L(\omega)$，范围从 -60 到 80。图中包含 $L(\omega)$（未校正）、$L_c(\omega)$（校正网络）和 $L''(\omega)$（已校正）曲线。关键点标注：$\omega_c'=12.6$ 处，$L(\omega)$ 曲线与 0dB 线相交。图中显示了斜率变化，包括 -20dB/dec 和 -40dB/dec 的段落。]

图 6-23 例 6-5 系统开环对数幅频特性

其次，若采用串联滞后校正，可以使系统的相角裕度提高到 $45^\circ$ 左右，但是对于本例高性能系统，会产生两个很严重的缺点：①滞后网络时间常数太大，这是因为静态速度误差系数越大，所需要的滞后网络时间常数越大。对于本例，要求选 $\omega_c''=1$，相应的 $L'(\omega_c'')=45.1\text{dB}$，根据式(6-41)求出 $b=1/200$，若 $1/(bT)=0.1\omega_c''$，可得 $T=2000\text{s}$，这样大的时间常数，实际上是无法实现的；②响应速度指标不满足，由于滞后校正极大减小了系统的截止频率，使得系统响应滞缓。对于本例，粗略估算的调节时间约为 $9.6\text{s}$，该值远大于性能指标的要求值。

上述论证表明，纯超前校正及纯滞后校正都不宜采用，应选用串联滞后-超前校正。

为了利用滞后-超前校正的超前部分微分的特性，研究图 6-23 发现，可取 $\omega_b=2\text{rad/s}$，于是待校正系统对数幅频特性在 $\omega \le 6\text{rad/s}$ 区间，其斜率均为 $-20\text{dB/dec}$。

根据 $t_s \le 3\text{s}$ 和 $\gamma''=45^\circ$ 的指标要求，不难算得 $\omega_c'' \ge 3.2\text{rad/s}$。考虑到要求中频区斜率为 $-20\text{dB/dec}$，故 $\omega_c''$ 应在 $3.2 \sim 6\text{rad/s}$ 范围内选取。由于 $-20\text{dB/dec}$ 的中频区应占据一定宽度，故选 $\omega_c''=3.5\text{rad/s}$，相应的 $L'(\omega_c'') + 20\lg T_b\omega_c'' = 34\text{dB}$。由式(6-43)可算出 $1/\alpha=0.02$，此时，滞后-超前网络的频率特性可写为
$$G_c(j\omega) = \frac{(1+j\omega/\omega_a)(1+j\omega/\omega_b)}{(1+j\alpha\omega/\omega_b)(1+j\omega/(\alpha\omega_b))} = \frac{(1+j\omega/\omega_a)(1+j\omega/2)}{(1+j50\omega/\omega_a)(1+j\omega/100)}$$
相应的已校正系统的频率特性为
$$G_c(j\omega)G_0(j\omega) = \frac{180(1+j\omega/\omega_a)}{j\omega(1+j\omega/6)(1+j\omega/\omega_a)(1+j\omega/100)}$$
根据上式，利用相角裕度指标要求，可以确定校正网络参数 $\omega_a$。已校正系统的相角裕度
$$\gamma'' = 180^\circ + \arctan\frac{\omega_c''}{\omega_a} - 90^\circ - \arctan\frac{\omega_c''}{6} - \arctan\frac{50\omega_c''}{\omega_a} - \arctan\frac{\omega_c''}{100}$$
$$= 57.7^\circ + \arctan\frac{3.5}{\omega_a} - \arctan\frac{175}{\omega_a}$$