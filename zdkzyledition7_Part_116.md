续表

| 序号 | 系统结构图 | $C(z)$计算式 |
| :--- | :--- | :--- |
| 6 | ![Diagram 6](https://i.imgur.com/gK2tW5c.png) | $\frac{RG(z)}{1+HG(z)}$ |
| 7 | ![Diagram 7](https://i.imgur.com/gK2tW5c.png) | $\frac{R(z)G(z)}{1+G(z)H(z)}$ |
| 8 | ![Diagram 8](https://i.imgur.com/gK2tW5c.png) | $\frac{G_1(z)G_2(z)R(z)}{1+G_1(z)G_2(z)H(z)}$ |

### 6. $z$ 变换法的局限性

$z$ 变换法是研究线性定常离散系统的一种有效工具，但是 $z$ 变换法也有其本身的局限性，应用 $z$ 变换法分析线性定常离散系统时，必须注意以下几方面问题。

(1) $z$ 变换的推导是建立在假定采样信号可以用理想脉冲序列来近似的基础上，每个理想脉冲的面积，等于采样瞬时上的时间函数。这种假定，只有当采样持续时间与系统的最大时间常数相比是很小的时候，才能成立。

(2) 输出 $z$ 变换函数 $C(z)$，只确定了时间函数 $c(t)$ 在采样瞬时上的数值，不能反映 $c(t)$ 在采样间隔中的信息。因此对于任何 $C(z)$，$z$ 反变换 $c(nT)$ 只能代表 $c(t)$ 在采样瞬时 $t=nT(n=0,1,2,\cdots)$ 时的数值。

(3) 用 $z$ 变换法分析离散系统时，系统连续部分传递函数 $G(s)$ 的极点数至少要比其零点数多两个，即 $G(s)$ 的脉冲过渡函数 $K(t)$ 在 $t=0$ 时必须没有跳跃，或者满足
$$\lim_{s \to \infty} sG(s) = 0 \quad (7-72)$$
否则，用 $z$ 变换法得到的系统采样输出 $c^*(t)$ 与实际连续输出 $c(t)$ 差别较大，甚至完全不符。

**例 7-21** 设 RC 积分网络如图 7-27 所示，其输入 $r(t)=1(t)$，采样周期 $T=1s$，试比较 $c^*(t)$ 与 $c(t)$。

**解** 网络传递函数
$$G_s(s) = \frac{1}{s+1}$$
相应的 $z$ 变换为
$$G(z) = \frac{z}{z-e^{-T}} = \frac{z}{z-0.368}$$
而输入 $z$ 变换
$$R(z) = \frac{z}{z-1}$$
因此，网络输出 $z$ 变换函数
$$C(z) = G(z)R(z) = \frac{z^2}{(z-1)(z-0.368)}$$
$$= 1+1.368z^{-1}+1.5z^{-2}+1.55z^{-3}+1.56z^{-4}+\cdots$$
上式取 $z$ 反变换，得输出采样信号
$$c^*(t) = \delta(t)+1.368\delta(t-1)+1.5\delta(t-2)+1.55\delta(t-3)+1.56\delta(t-4)+\cdots$$
于是，可以画出 $c(t)$ 在采样瞬时的值 $c(nT)(T=1, n=0,1,2,\cdots)$，如图 7-28(a) 所示。如果采用拉氏变换的方法，求出当连续部分的输入为 $r^*(t)=\delta(t)$ 时，系统的连续输出 $c(t)$，如图 7-28(b) 所示。

由图可见，在 $c^*(t)$ 曲线中，将其采样瞬时的幅值连接起来所画出的光滑曲线，与实际的 $c(t)$ 有显著差别，这是因为此时 $G(s)$ 不满足式(7-72)。

**图 7-28 积分网络的采样输出响应和连续输出响应(MATLAB)**
(a) 采样输出：横轴为 $t$，纵轴为 $c^*(t)$。曲线显示为阶梯状，在 $t=0, T, 2T, \dots, 8T$ 处有阶跃，数值从 1 逐渐上升至 1.582。
(b) 连续输出：横轴为 $t$，纵轴为 $c(t)$。曲线显示为平滑的指数上升曲线，从 0 开始，在 $t=8T$ 时达到 1.582。

---

### 7-5 离散系统的稳定性与稳态误差

正如在线性连续系统分析中的情况一样，稳定性和稳态误差也是线性定常离散系统分析的重要内容。本节主要讨论如何在 $z$ 域和 $w$ 域中分析离散系统的稳定性，同时给出计算离散系统在采样瞬时稳态误差的方法。

为了把连续系统在 $s$ 平面上分析稳定性的结果移植在 $z$ 平面上分析离散系统的稳定性，首先需要研究 $s$ 平面与 $z$ 平面的映射关系。

#### 1. $s$ 域到 $z$ 域的映射

在 $z$ 变换定义中，$z=e^{sT}$ 给出了 $s$ 域到 $z$ 域的关系。$s$ 域中的任意点可表示为 $s=\sigma+j\omega$，映射到 $z$ 域则为
$$z = e^{(\sigma+j\omega)T} = e^{\sigma T}e^{j\omega T}$$
于是，$s$ 域到 $z$ 域的基本映射关系式为
$$|z| = e^{\sigma T}, \quad \angle z = \omega T \quad (7-73)$$

令 $\sigma=0$，相当于 $s$ 平面的虚轴，当 $\omega$ 从 $-\infty$ 变到 $\infty$ 时，由式(7-73)知，映射到 $z$ 平面的轨迹是以原点为圆心的单位圆。只是 $s$ 平面上的点沿虚轴从 $-\infty$ 移到 $\infty$ 时，$z$ 平面上的相应点已经沿着单位圆转过了无穷多圈。这是因为当 $s$ 平面上的点沿虚轴从 $-\omega_s/2$ 移动到 $\omega_s/2$ 时，其中 $\omega_s$ 为采样频率，$z$ 平面上的相应点又将逆时针沿单位圆转过一圈(见式(7-73)中 $\angle z$ 计算式)，正好转了一圈；而 $s$ 平面上的点在虚轴上从 $\omega_s/2$ 移动到 $3\omega_s/2$ 时，$z$ 平面上的相应点又将逆时针沿单位圆转过一圈。以此类推，如图 7-29 所示。由图可见，可以把 $s$ 平面划分为无穷多条平行于实轴的周期带，其中 $-\omega_s/2$ 到 $\omega_s/2$ 的周期带称为主带，其余的周期带叫做次带。为了研究 $s$ 平面上的主带在 $z$ 平面上的映射，可分以下几种情况讨论。

**(1) 等 $\sigma$ 线映射**
$s$ 平面上的等 $\sigma$ 线，映射到 $z$ 平面上的轨迹，是以原点为圆心，以 $|z|=e^{\sigma T}$ 为半径的圆，其中 $T$ 为采样周期，如图 7-30 所示。由于 $s$ 平面上的虚轴映射为 $z$ 平面上的单位圆，所以左半 $s$ 平面上的等 $\sigma$ 线映射为 $z$ 平面上的同心圆，在单位圆内；右半 $s$ 平面上的等 $\sigma$ 线映射为 $z$ 平面上的同心圆，在单位圆外。

**图 7-29 $s$ 平面虚轴在 $z$ 平面的映射**：左侧为 $s$ 平面，显示虚轴上的主带和次带；右侧为 $z$ 平面，显示单位圆。
**图 7-30 $s$ 平面和 $z$ 平面上的等 $\sigma$ 轨迹**：(a) $s$ 平面显示 $\sigma_1, \sigma_2$ 线；(b) $z$ 平面显示对应的同心圆。

**(2) 等 $\omega$ 线映射**
在特定采样周期 $T$ 下，由式(7-73)可知，$s$ 平面上的等 $\omega$ 平线映射到 $z$ 平面上的轨迹，是一条从原点出发的射线，其相角 $\angle z = \omega T$ 从正实轴计量，如图 7-31 所示。由图可见，$s$ 平面 $\omega=\omega_s/2$ 水平线，在 $z$ 平面上正好映射为负实轴。

**(3) 等 $\zeta$ 线映射**
$s$ 平面上的等 $\zeta$ 线可用下式描述：
$$s = -\omega \tan \beta + j\omega$$
其中，$\beta$ 为 $\zeta$ 线与虚轴之间的夹角。于是...

**图 7-31 $s$ 平面和 $z$ 平面上的等 $\omega$ 轨迹**：(a) $s$ 平面显示 $\omega_1, \omega_2$ 水平线；(b) $z$ 平面显示对应的射线。