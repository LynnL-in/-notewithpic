不难验算 $E_2(z)=D(z)\Phi_e(z)R(z)=0.383z^{-1}+0.0172z^{-2}+0.1(z^{-3}+z^{-4}+\cdots)$
数字控制器的输出序列
$e_2(0)=0, \quad e_2(T)=0.383, \quad e_2(2T)=0.017, \quad e_2(3T)=e_2(4T)=\cdots=0.1$
系统从第三拍起，$e_2(nT)$达到稳态，输出没有纹波，说明所求出的控制器 $D(z)$ 是合理的。此时系统为三拍系统。与最少拍设计相比，所增加的一拍正好是 $G(z)$ 在单位圆内的零点数。

### 4. PID 数字控制器的实现

在数字校正装置 $D(z)$ 的实现方法中，常采用 PID 数字控制器。PID 控制器的传递函数为
$$D(s)=\frac{U(s)}{X(s)}=K_1+\frac{K_2}{s}+K_3s$$
将其中的微分项和积分项进行离散化处理，就可以确定 PID 控制器的数字实现。
对微分项，应用向后差分法，有
$$u(kT)=\left.\frac{dx}{dt}\right|_{t=kT}=\frac{1}{T}\{x(kT)-x[(k-1)T]\}$$
上式的 $z$ 变换为
$$U(z)=\frac{1-z^{-1}}{T}X(z)=\frac{z-1}{Tz}X(z)$$
对积分项，同样有
$$u(kT)=u[(k-1)T]+Tx(kT)$$
上式的 $z$ 变换为
$$U(z)=z^{-1}U(z)+TX(z)$$
整理得
$$U(z)=\frac{Tz}{z-1}X(z)$$
因此，PID 控制器在 $z$ 域的传递函数为
$$D(z)=\frac{U(z)}{X(z)}=K_1+K_2\frac{Tz}{z-1}+K_3\frac{z-1}{Tz}$$
若记 $x(kT)=x(k)$，则可得 PID 控制器的差分方程：
$$u(k)=K_1x(k)+K_2[u(k-1)+Tx(k)]+\frac{K_3}{T}[x(k)-x(k-1)]$$
$$=\left[K_1+K_2T+\frac{K_3}{T}\right]x(k)-\frac{K_3}{T}x(k-1)+K_2u(k-1)$$
采用计算机软件，可以方便地实现上述 PID 数字控制器。显然，令 $K_2$ 或 $K_3$ 分别为零，可得 PD 或 PI 数字控制器。

---
· 365 ·

---

## 7-8 离散控制系统设计

**例 7-31** 二阶采样系统的性能。
有零阶保持器的二阶系统如图 7-48 所示，其中被控对象的传递函数为
$$G_0(s)=\frac{K}{s(T_1s+1)}$$
采样周期为 $T$，则开环脉冲传递函数为
$$G(z)=(1-z^{-1})\mathcal{Z}\left[\frac{G_0(s)}{s}\right]=\frac{K\frac{T}{T_1}\left(e^{-T/T_1}+T/T_1-1\right)z+(1-\frac{T}{T_1}e^{-T/T_1}-e^{-T/T_1})}{(z-1)(z-e^{-T/T_1})}$$
若令 $E=e^{-T/T_1}$，则上式可表示为
$$G(z)=\frac{K[(ET_1+T-T_1)z+(T_1-TE-T_1E)]}{(z-1)(z-E)}$$

**[图 7-48 闭环采样系统结构图]**
*图示说明：该图展示了一个闭环控制系统。输入信号 $r(t)$ 进入求和点，产生误差信号 $e(t)$。$e(t)$ 经过采样器变为 $e^*(t)$，随后依次通过零阶保持器 $G_h(s)$ 和对象 $G_0(s)$，最终输出 $c(t)$。输出 $c(t)$ 被反馈回求和点与 $r(t)$ 相减。*

闭环特征方程为
$$D(z)=z^2+z\{K[T-T_1(1-E)]-(1+E)\}+K[T_1(1-E)-TE]+E=0$$
这是一个实系数的一元二次方程，由朱利稳定判据知，由于 $n=2$，且
$$a_0=K[T_1(1-E)-TE]+E, a_1=K[T-T_1(1-E)]-(1+E), a_2=1$$
故两个特征根都位于 $z$ 平面上单位圆内的充分必要条件为
$$D(1)>0, D(-1)>0$$
并满足约束条件 $|a_0|<a_2$。
当 $K>0$ 及 $T>0$ 时，可由 $|a_0|<1$ 及 $D(-1)>0$ 导出上述二阶采样系统稳定性的等价必要条件
$$KT_1 < \frac{1-E}{1-E-\frac{T}{T_1}E}$$
以及
$$KT_1 < \frac{2(1+E)}{\frac{T}{T_1}(1+E)-2(1-E)}$$
根据稳定性的必要条件，可以计算稳定系统所容许的最大增益。表 7-8 给出了 $T/T_1$ 为不同取值时所对应的最大增益。由表 7-8 可见，当计算机有足够的运算速度时，可

· 366 ·

---

取 $T/T_1=0.1$。在此条件下，离散系统的增益上限值较大，其系统特性与连续系统基本一致。

**表 7-8 二阶采样系统的最大增益**

| $T/T_1$ | 0 | 0.01 | 0.1 | 0.5 | 1.0 | 2.0 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| $E$ | 1.0 | 0.990 | 0.905 | 0.607 | 0.368 | 0.135 |
| $(KT_1)_{\max}$ | $\infty$ | 100 | 21.11 | 4.39 | 2.39 | 1.45 |

当增益 $K$ 和采样周期 $T$ 发生变化时，二阶采样系统的阶跃响应最大超调量如图 7-49 所示。

**[图 7-49 二阶采样系统阶跃响应的最大超调量]**
*图示说明：该图为一张坐标图，横坐标为 $T/T_1$，纵坐标为 $KT_1$。图中绘制了多条代表不同超调量 $\sigma$ 的等值线（从 $\sigma=0.1$ 到 $\sigma=1.0$）。图的右上角标有“稳定极限”曲线，该曲线将稳定区域与不稳定区域分开。随着 $T/T_1$ 的增大，系统允许的 $KT_1$ 值减小。*

由 $G(z)$ 表达式可见，本例为 I 型系统，在单位斜坡输入作用下，其稳态跟踪误差 $e_{ss}(\infty)$ 可由式(7-81)算得
$$e_{ss}(\infty)=\frac{T}{K_v}$$
式中，$K_v$ 可由式(7-82)确定为
$$K_v=\lim_{z\to 1}(z-1)G(z)$$
显然，对于给定的 $T/T_1$，增大 $KT_1$ 的取值，可以减小系统跟踪斜坡响应的稳态误差，但同时也会使系统阶跃响应的超调量增大，从而使调节时间加长。

· 367 ·