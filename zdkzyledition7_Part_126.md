## Page 1

现在设图 7-48 采样系统中的被控对象为
$$G_0(s) = \frac{K}{s(0.1s+1)(0.005s+1)}$$
要求确定增益 $K$ 和采样周期 $T$ 的合适取值，使采样系统阶跃响应的超调量不大于 30%。

**解** 由题可知，$T_1=0.1\text{s}$, $T_2=0.005\text{s}$, $T_2$ 仅为 $T_1$ 的 5%，其影响可略去，因此该系统可近似为二阶采样系统。

若取 $T/T_1=0.25$, $\sigma=0.3$，则由图 7-49 可得 $KT_1=1.36$。因 $T_1=0.1\text{s}$，故求得 $K=13.6$, $T=0.025\text{s}$。此时，系统需每秒采样 40 次。

根据 $K=13.6$, $T=0.025\text{s}$，由式(7-82)算出 $e_{ss}(\infty)=0.0735$。

如果改取 $T/T_1=0.1$，则可望进一步减小系统阶跃响应的超调量和斜坡响应的稳态误差。

MATLAB 验证：令采样周期 $T=0.025\text{s}$，增益 $K=13.6$，对图 7-48 采样系统进行 MATLAB 仿真，其中 $G_0(s)=\frac{13.6}{s(0.1s+1)(0.005s+1)}$，得采样系统单位阶跃响应曲线及其相应的 MATLAB 文本，如图 7-50 所示，测得 $\sigma\%=30\%$, $t_p=0.3\text{s}$, $t_s=1.28\text{s}(\Delta=2\%)$。

若取 $T/T_1=0.1$，即 $T=0.01\text{s}$, $K=13.6$，则单位阶跃响应曲线及其相应的 MATLAB 文本，如图 7-51 所示，测得 $\sigma\%=28.7\%$, $t_p=0.3\text{s}$, $t_s=0.94\text{s}(\Delta=2\%)$。

**(a) MATLAB 文本 (图 7-50)**
```matlab
T=0.025; T1=0.1; T2=0.005; K=13.6;
G=zpk([],[0 -1/T1 -1/T2],13.6/(T1*T2));
Gz=c2d(G,T,'zoh');
sys=feedback(Gz,1);
t=0:T:2;step(sys,t);grid
```

**(a) MATLAB 文本 (图 7-51)**
```matlab
T=0.01; T1=0.1; T2=0.005; K=13.6;
G=zpk([],[0 -1/T1 -1/T2],13.6/(T1*T2));
Gz=c2d(G,T,'zoh');
sys=feedback(Gz,1);
t=0:T:2;step(sys,t);grid
```

**图 7-50 (b) 单位阶跃响应**：横轴为 Time/sec (0 到 2.0)，纵轴为 Amplitude (0 到 1.4)。曲线从 0 开始，经过一次超调后在 1.0 附近震荡并趋于稳定。

**图 7-51 (b) 单位阶跃响应**：横轴为 Time/sec (0 到 2.0)，纵轴为 Amplitude (0 到 1.4)。曲线从 0 开始，超调较小，在 1.0 附近趋于稳定。

**图 7-50** 当 $T=0.025\text{s}$, $K=13.6$ 时二阶采样的系统的时间响应(MATLAB)
**图 7-51** 当 $T=0.01\text{s}$, $K=13.6$ 时二阶采样的系统的时间响应(MATLAB)

---

## Page 2

**例 7-32** 工作台数字控制系统。
在制造业中，工作台运动控制系统是一个重要的定位系统，可以使工作台运动至指定的位置。工作台在每个轴上由电机和导引螺杆驱动，其中 $x$ 轴上的运动控制系统框图如图 7-52 所示。

现要求设计数字控制器 $D(z)$，使系统满足如下性能：
1) 超调量不大于 5%；
2) 具有较小的上升时间和调节时间($\Delta=2\%$)。

**解** 首先确定与图 7-52 相应的连续系统控制模型，如图 7-53 所示。以连续系统为基础，设计合适的控制器 $G_c(s)$，然后将 $G_c(s)$ 转换为要求的数字控制器 $D(z)$。

**图 7-52 (a) 执行机构和工作台**：框图显示位置命令 $r(t)$ 进入计算机，经过零阶保持器，驱动电机，通过接头带动导引螺杆，最终驱动工作台，传感器反馈实际位置 $x(t)$。
**图 7-52 (b) 系统结构图**：$R(s)$ 进入求和点，经过 $D(z)$，再经过零阶保持器和对象 $G_0(s) = \frac{1}{s(s+10)(s+20)}$，输出 $X(s)$。

为了确定未校正系统的响应，先将控制器取为简单的增益 $K^*$，以 $K^*$ 为可变参数绘制系统的根轨迹，如图 7-54 所示，从中可得：当根轨迹增益 $K^*=641$，即开环增益 $K=3.2$ 时，系统主导极点 $s_{1,2}=-3.72\pm j3.72$ 的阻尼比 $\zeta=0.707$。经仿真可得系统单位阶跃响应的性能，如表 7-9 中第一行所示。此时，系统的调节时间较长，系统时间响应及其相应的 MATLAB 文本，如图 7-55 所示。

**表 7-9 采用不同控制器时连续系统的响应性能**

| 控制器 $G_c(s)$ | $K^*$ | 超调量/% | 调节时间/s | 上升时间/s |
| :--- | :--- | :--- | :--- | :--- |
| $K^*$ | 641 | 4.33 | 1.25 | 0.68 |
| $\frac{K^*(s+11)}{s+62}$ | 7800 | 4.33 | 0.58 | 0.32 |

---

## Page 3

**图 7-53 工作台的支撑控制模型结构图**：$R(s)$ 进入求和点，经过控制器 $G_c(s)$，功率放大器 $\frac{1}{s+20}$，电机 $\frac{1}{s(s+10)}$，输出 $X(s)$。

**图 7-54 $1+\frac{K^*}{s(s+10)(s+20)}=0$ 的根轨迹(MATLAB)**：显示了复平面上的根轨迹图，标注了 $s=-3.72\pm j3.72$ 处的阻尼比 $\zeta=0.707$。

**图 7-55 (a) MATLAB 文本**
```matlab
G0=tf([641], [1, 30, 200, 0]); sys=feedback(G0, 1);
% 建立闭环系统
t=0:0.01:2; step(sys, t); % 绘制阶跃响应曲线
```

**图 7-55 (b) 系统时间响应**：横轴为 Time/sec (0 到 2.0)，纵轴为 Amplitude (0 到 1.4)。曲线平滑上升并在 1.0 处趋于稳定。

其次，将控制器取为超前校正网络，即
$$G_c(s) = \frac{K^*(s+a)}{s+b}$$
为了保证预期主导极点的主导特性，取 $a=11$, $b=62$。于是在根轨迹图 7-56 上可以确定阻尼比 $\zeta=0.707$ 的主导极点为 $s_{1,2}=-7.16\pm j7.16$。

系统的根轨迹增益 $K^*=7800$，即系统开环增益 $K=6.92$，系统的单位阶跃响应及其相应的 MATLAB 文本如图 7-57 所示。仿真表明，校正后系统具有满意的性能，其性能指...