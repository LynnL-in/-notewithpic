## 第八章 非线性控制系统分析

### 8-1 非线性控制系统概述

**1. 研究非线性控制理论的意义**

以上章节详细讨论了线性定常控制系统的分析和设计问题。但实际上，理想的线性系统并不存在，因为组成控制系统的各元件的动态和静态特性都存在着不同程度的非线性。以随动系统为例，放大元件由于受电源电压或输出功率的限制，在输入电压超过放大器的线性工作范围时，输出会呈饱和现象，如图 8-1(a)所示；执行元件电动机，由于轴上存在摩擦力矩和负载力矩，只有在电极电压达到一定数值后，电机才会转动，存在着死区，而当电极电压超过一定数值时，电机的转速将不再增加，出现饱和现象，其特性如图 8-1(b)所示；又如传动机构，受加工和装配精度的限制，换向时存在着间隙特性，如图 8-1(c)所示。

*   **图 8-1 几何典型的非线性特性**
    *   (a) 饱和特性：坐标系中，曲线从原点出发，先呈线性增长，随后在正负方向上趋于水平，表示输出饱和。
    *   (b) 有死区的饱和特性：坐标系中，曲线在原点附近的一段区间内为零（死区），随后呈线性增长，最后趋于水平（饱和）。
    *   (c) 间隙特性：坐标系中，曲线表现为具有回环的死区特性。

*   **图 8-2 液位系统原理图**
    *   显示了一个圆柱形储液槽，输入流量为 $Q_i$，输出流量为 $Q_o$，液位高度为 $H$。

实际系统中普遍存在非线性因素。当系统中含有一个或多个具有非线性特性的元件时，该系统称为非线性系统。例如，在图 8-2 所示的柱形液位系统中，设 $H$ 为液位高度，$Q_i$ 为液体流入量，$Q_o$ 为液体流出量，$C$ 为贮槽的截面积。根据水力学原理
$$Q_o = k\sqrt{H} \quad (8-1)$$
其中比例系数 $k$ 取决于液体的黏度和阀阻。液位系统的动态方程为
$$C\frac{dH}{dt} = Q_i - Q_o = Q_i - k\sqrt{H} \quad (8-2)$$
显然，液位 $H$ 和液体输入量 $Q_i$ 的数学关系式为非线性微分方程。一般地，非线性系统的数学模型可以表示为
$$f\left(t, \frac{d^n y}{dt^n}, \dots, \frac{dy}{dt}, y\right) = g\left(t, \frac{d^m r}{dt^m}, \dots, \frac{dr}{dt}, r\right) \quad (8-3)$$
其中 $f(\cdot)$ 和 $g(\cdot)$ 为非线性函数。

---

### 习题部分 (第 377-378 页)

**要求：**
(1) 设计合适的连续控制器 $G_c(s) = \frac{s+a}{s+b}$，使系统的相角裕度 $\gamma \ge 45^\circ$；
(2) 选择采样周期 $T=0.01s$，采用 $G_c(s)-D(z)$ 变换方法，求出相应的数字控制器 $D(z)$。

*   **图 7-69 足球场上的移动摄像机示意图**：展示了摄像机安装在滑轮上，通过电机驱动。
*   **图 7-70 滑轮上的电机控制系统结构图**：包含 $r(t) \to \text{比较器} \to D(z) \to T \to G_h(s) \to G_0(s) \to c(t)$ 的反馈回路。

**7-23** 设数字控制系统如图 7-71 所示，其中 $G(z)$ 包括了零阶保持器和被控对象。已知被控对象
$$G_0(s) = \frac{1}{s(s+10)}$$
若采样周期 $T=0.1s$，要求：
(1) 当 $D(z)=K$ 时，计算脉冲传递函数 $G(z)D(z)$；
(2) 求闭环系统的 $z$ 特征方程；
(3) 计算使系统稳定的 $K$ 的最大值；
(4) 确定 $K$ 的合适值，使系统的超调量不大 于 30%；
(5) 采用(4)中得到的增益 $K$，计算闭环脉冲传递函数 $\Phi(z)$，并绘出系统的单位阶跃响应曲线；
(6) 取 $K=0.5K_{\max}$，求系统闭环极点及超调量；
(7) 在(6)所给出的条件下，画出系统的单位阶跃响应曲线。

*   **图 7-71 数字控制系统结构图**：包含 $r(t) \to \text{比较器} \to D(z) \to T \to G(z) \to c(t)$ 的反馈回路。

**7-24** 设连续的、未经采样的控制系统如图 7-72 所示，其中被控对象
$$G_0(s) = \frac{1}{s(s+10)}$$
要求：
(1) 设计滞后校正网络
$$G_c(s) = K\frac{s+a}{s+b} \quad (a>b)$$
使系统在单位阶跃输入时的超调量 $\sigma\% \le 30\%$，且在单位斜坡输入时的稳态误差 $e_{ss}(\infty) \le 0.01$；
(2) 若为该系统增配一套采样器和零阶保持器，并选采样周期 $T=0.1s$，试采用 $G_c(s)-D(z)$ 变换方法，设计合适的数字控制器 $D(z)$；
(3) 分别画出(1)及(2)中连续系统和离散系统的单位阶跃响应曲线，并比较两者的结果；
(4) 另选采样周期 $T=0.01s$，重新完成(2)和(3)的工作；
(5) 对于(2)中得到的 $D(z)$，画出离散系统的单位斜坡响应，并与连续系统的单位斜坡响应进行比较。

*   **图 7-72 控制系统结构图**：包含 $r(t) \to \text{比较器} \to e(t) \to G_c(s) \to G_0(s) \to c(t)$ 的反馈回路。

**7-25** 设闭环离散系统如图 7-73 所示，若采样周期在 $0 \le T \le 1.2s$ 范围内变化，试在 $T$ 每增加 $0.2s$ 之后，绘出系统的单位阶跃输入响应，要求列表记录相应的 $\sigma\%$ 和 $t_s(\Delta=2\%)$。

*   **图 7-73 闭环离散系统结构图**：包含 $r(t) \to \text{比较器} \to e(t) \to e^*(t) \to \frac{1-e^{-sT}}{s} \to \frac{1}{s(s+1)} \to c(t)$ 的反馈回路。

**7-26** 设具有采样器、零阶保持器的闭环离散系统如图 7-74 所示，当采样周期 $T=0.1s$，输入信号为单位阶跃信号时，试计算系统的输出 $C(z)$。

*   **图 7-74 闭环离散系统结构图**：包含 $r(t) \to \text{比较器} \to e(t) \to \frac{1-e^{-sT}}{s} \to \frac{10}{s-2} \to c(t)$ 的反馈回路。